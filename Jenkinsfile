pipeline {
  agent any
  stages {
    stage('Dev Deployment and Run') {
      steps {
        sshagent(credentials: ['dev-ssh-credentials']) {
          sh '''
                    '''
        }

      }
    }

  }
}